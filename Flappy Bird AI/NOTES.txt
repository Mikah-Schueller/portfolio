LONG STATE
state = [
   # Bird above opening
   height < opening_closest_pipe - 32,
   # Bird below opening
   height > opening_closest_pipe + 32,
   # Bird close to bottom pipe
   (height < opening_closest_pipe + 45 and height >= opening_closest_pipe + 40),

   # Bird in pipe
   current_pipe.position - 94 < 0,
   # Velocity negative
   velocity < 0,

   # Close to bottom
   height + 13 > 470,
   # Close to top
   height - 13 < 10
]

SHORT STATE
FULL STATE
state = [
   # Bird above opening
   height < opening_closest_pipe - 32,
   # Bird below opening
   height > opening_closest_pipe + 32,
   # Bird close to bottom pipe
   (height < opening_closest_pipe + 32 and height >= opening_closest_pipe + 27)
]